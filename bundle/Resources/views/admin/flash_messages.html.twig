{% for message in app.session.flashBag.get('netgen_information_collection.success') %}
    <div class="ng-ui-message ng-ui-message-success">
        {{ message }}
    </div>
{% endfor %}

{% for message in app.session.flashBag.get('netgen_information_collection.errors') %}
    <div class="ng-ui-message ng-ui-message-error">
        {{ message }}
    </div>
{% endfor %}
