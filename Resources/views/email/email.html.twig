{% set vars =
    {
        'content': content,
        'content_type': content_type,
        'params': params,
        'additional_content': additional_content
    }
%}

{%
    include ezpublish.configResolver.hasParameter( 'information_collection.email.' ~ content_type.identifier, 'netgen' ) ?
        ezpublish.configResolver.getParameter( 'information_collection.email.' ~ content_type.identifier, 'netgen' ) :
        ezpublish.configResolver.getParameter( 'information_collection.email.default', 'netgen' )
    with vars
%}

